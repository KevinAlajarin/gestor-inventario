# Backend API - Inventory System
Node.js + Express + SQL Server (MSSQL)

## Setup
1. Instalar dependencias: `npm install`
2. Configurar `.env` basado en `.env.example`
3. Ejecutar script SQL en tu SQL Server.
4. Correr: `npm run dev`